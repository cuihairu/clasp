# vcpkg Port Draft

This directory is a draft for a future vcpkg port.

Before submitting to `microsoft/vcpkg`, replace `SHA512 0` in `portfile.cmake`
with the hash reported by vcpkg after `v0.1.0` has been pushed to GitHub.
